pipeline:
  stages:
    - name: stage_9bfd
      id: stage_9bfd
      steps:
        - name: run1
          run:
            script: |-
              echo "using pipeline from non main branch"

              exit 0
          id: run1
