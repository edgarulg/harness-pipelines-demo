template:
  step:
    run:
      script: echo "$MESSAGE from remote template ref"
      env:
        MESSAGE: ${{inputs.message}}
  inputs:
    message:
      type: string
      label: message
      required: true
